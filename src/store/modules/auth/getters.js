export default {
  getters: {
    userId(state) {
      return state.userId;
    },
    supId(state) {
      return state.supId;
    },
    workoutId(state) {
      return state.workoutId;
    },
  },
};
